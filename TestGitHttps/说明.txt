TestGit 使用的SSH方式提交，没问题，应clone仓库的时候就是使用的SSH地址，使用HTTPS方式就死活push不上去
TestGitHttps Egit clone使用的HTTPS地址，看看使用HTTPS方式可以提交不 真的是这样的，Git Repository视图clone使用的什么方式，
以后项目的提交就只能使用什么方式了

试试提交到别的仓库--提交不成功

TestGit 是SSH方式的仓库，我使用HTTPS方式提交成功了，但是没有输入用户和密码，这是怎么回事？
我在TestGitHttps项目使用HTTPS方式的参股，使用HTTPS方式提交，输入用户和密码，看看能不能提交成功?

这我就郁闷了啦，我输入用户和密码是可以正常提交的，用户名是：宁创新，密码是:1259*8*n**